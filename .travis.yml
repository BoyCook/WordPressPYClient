language: python
python:
- 2.7

install:
- pip install --use-mirrors \
    `python -c 'from setup import REQUIREMENTS; print " ".join(REQUIREMENTS["testing"] + REQUIREMENTS["install"])'`

script: make test
